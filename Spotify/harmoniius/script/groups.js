/*
 *
 * Harmonic Residence Groups Controller
 */
 
//Post: Create a group
function createGroup(name, description, host_id) {
	//Call our own server
	//Get back a group id
} 

//Post: Add a user to the Group
function addUserToGroup(group_id, user_id) {
	//Add a user to the group
}

//Post: Gets the number of users of a particular group
function getNumUsers(group_id) {
	//Call our server to get number of users
}